# 发布前用户测试清单

> **约定**：从 v1.3.0 之后，每个新版本 release 都先经过下方清单的人工验证，
> 用户（AmazingBoyCrazy）确认所有项目 OK 后，AI 才执行打包和发布。

---

## A. 必测项（每次都跑）

### 1. 基础安装 / 启动
- [ ] 解压 `xyz2md_dir.zip` 到任意目录（或下载 `xyz2md.exe`）
- [ ] **不复制模型目录**，首次运行会下载
- [ ] 双击 `xyz2md.exe`（或 `xyz2md_dir.exe`）能正常打开窗口，无错误弹窗
- [ ] 关闭再打开仍正常

### 2. 配置页 → 转写页 → 美化页 三页流转
- [ ] 粘贴一个**已知有效的单集链接**（如：`https://www.xiaoyuzhoufm.com/episode/6a6ff6f5ab3a91c24a0ec11e`）
- [ ] 选 `small` 模型 + 默认输出目录
- [ ] 点「开始转换」→ 立刻跳转到转写页
- [ ] 转写页**头部应快速显示**：单集标题、播客名、封面图
- [ ] 进度条随转写推进（每 ~20 段日志更新一次）
- [ ] 转写完成后**自动跳到美化页**
- [ ] 美化页显示：✅ 完成、标题、段数、耗时
- [ ] 美化页繁简状态正确显示"已自动转换为简体中文"

### 3. 文件产出
- [ ] 输出目录有 `{eid}_{标题}.md`
- [ ] MD 内容**无繁体专属字**（可对照 BEAUTIFY_SPEC §1 列出）
- [ ] MD 顶部有元数据 + 封面 + 时间轴章节

### 4. 下载稳定性
- [ ] 测试**一个时长 30 分钟以上的 episode**（不要只用 2 分钟的测）
- [ ] 观察下载日志：是否显示「下载中: X / Y MB (Z%)」进度
- [ ] 下载完成后文件大小应**等于 Content-Length**（在日志中可见）

---

## B. API 精修测试（v1.3.0+ 每次测）

- [ ] 在美化页选服务商（如 DeepSeek），填 API Key
- [ ] 点「保存」（应在 exe 旁生成 `config.json`）
- [ ] 关闭重开，API Key 应自动加载
- [ ] 点「🚀 开始精修」→ 进度条更新 + 日志逐段刷新
- [ ] 完成后生成 `{标题}_精修.md`，**有标点、无繁简混存**
- [ ] 测试「⏹ 停止」功能：精修中途点停，已处理段保留

---

## C. 错误处理测试

- [ ] 不填链接直接点「开始」：弹窗"请先粘贴小宇宙单集链接"
- [ ] 链接格式错（不是 episode 链接）：弹窗"链接格式不对"
- [ ] 转写中途点「⏹ 停止」：保留部分文字稿，状态变"⏹ 已停止"

---

## D. 环境清理 / 重新安装测试

- [ ] 删除 `models/` 后重新运行：应自动下载模型
- [ ] 删除 `config.json` 后重新运行：API Key 输入框应为空
- [ ] 把整个 `xyz2md_dir/` 目录拷给朋友电脑：能直接用（首次运行下载模型）

---

## E. 版本升级兼容测试

- [ ] 覆盖升级：从旧版本 `xyz2md_dir/` 直接覆盖 `xyz2md_dir.exe`
- [ ] 旧版的 `models/` 目录应保留复用，无需重新下载

---

## F. 命令行测试（开发者 / 高级用户）

- [ ] `xyz2md.exe --smoke`：2 秒后自动关闭，exit 0
- [ ] `xyz2md.exe --auto <url> --limit-minutes 3 --model tiny --out DIR`：完整自动转写 + 自动关闭
- [ ] `xyz2md.exe --cli <url> --no-audio`：仅生成元数据 MD
- [ ] `xyz2md.exe --cli <url> --limit-minutes 5 --model base`：换模型

---

## 验证确认后请回复

```
✅ 所有项验证通过, 可以发布 v1.X.Y
```

或

```
❌ [项号] 失败: 具体描述
```

AI 在收到 ✅ 后才执行：
1. 打包 onefile + onedir exe
2. 创建 GitHub Release
3. 上传资产

---

## 已知 / 待修

- v1.3.0: 特定 episode URL（`6a819b2517676351c572adc0`）在 DSH 沙箱内间歇性 WinError 10106，
  用户实际机器需要单独验证 — 这是网络环境差异，非代码缺陷。